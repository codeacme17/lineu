{
  "name": "lineu",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "watch": "pnpm -r --parallel watch",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "engines": {
    "pnpm": ">=8.0.0"
  }
}
